<!--
SPDX-License-Identifier: AGPL-3.0-or-later
Commercial license available
© Concepts 1996–2026 Miroslav Šotek. All rights reserved.
© Code 2020–2026 Miroslav Šotek. All rights reserved.
ORCID: 0009-0009-3560-0851
Contact: www.anulum.li | protoscience@anulum.li
SCPN RFP Core — benchmark record
-->

# Benchmarks

Every number here is regenerated by `benchmarks/level0_physics.py` per the
ecosystem benchmark standard (warm-up, repeated samples, percentiles, one
row per backend, provenance in the artefact). The operation is one pass
over a synthetic parameter grid evaluating, per point, the Bessel-function
relaxed state (force-free parameter, axis field, model reversal
parameter, reversal radius, edge-field comparison) and the profile at
five radial stations; the time is reported per grid point and includes
the Python call overhead of both backends (the native backend is called
per point and per station through its bindings, so this measures the
call-through cost of the kernels, not a vectorised scan). Both backends
evaluate the Bessel functions through the pinned shared kernel library
(ADR 0006). Nothing here is a physics claim.

## Level-0 physics grid pass — local workstation (non-isolated)

Artefact: `benchmarks/results/level0_physics.local.json`
(schema `scpn-rfp-core.level0-physics-benchmark.v1`, generated
2026-09-02T21:33:50.500017+00:00, at parent commit `f5a35424b055` with the
working tree of the landing commit).
Host: 11th Gen Intel(R) Core(TM) i5-11600K @ 3.90GHz,
Linux-7.0.0-30-generic-x86_64-with-glibc2.39, Python 3.12.3; load average
at start 9.91 (other work was running on the host); cores not isolated
(shared workstation, so treat the numbers as indicative).
Parameters: 100000 points, 3 warm-up passes, 20 timed passes.

| Backend | P50 µs/point | P95 µs/point | P99 µs/point | mean µs/point | throughput points/s | status |
|---|---|---|---|---|---|---|
| `python_floor` (public API, always available) | 34.738 | 38.216 | 38.337 | 35.149 | 28787 | measured |
| `rust_native` (optional build: `rust/`, maturin; fetches the pinned kernel crate) | 2.246 | 2.362 | 2.368 | 2.259 | 445302 | measured |

P50 speed-up of the native call-through over the Python floor: 15.5×.
The fast row requires the optional native module and is never the
default.

## Fixed-runner (CI) number

Not yet published: the hosted `rust` job runs a benchmark smoke that
proves both backends measure, but the canonical fixed-runner artefact
requires a dispatchable benchmark workflow, which is held under the
current Actions cost policy. Until it exists, the only published number
is the local, non-isolated one above.

## Regenerate

```bash
make rust
VIRTUAL_ENV=.venv PATH=.venv/bin:$PATH maturin develop --release -m rust/Cargo.toml
.venv/bin/python benchmarks/level0_physics.py --points 100000 --warmup 3 --repeats 20 --label local
```
